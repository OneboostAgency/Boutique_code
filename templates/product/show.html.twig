{% extends 'base.html.twig.html' %}

{% block title %}{{products.name}} - La boutique{% endblock %}


{% block content %}
    <div class="row">
        <div class="col-md-5">
            <img src="/uploads/{{products.illustration}}" alt="{{products.name}}" class="img-fluid" >
        </div>
        <div class="col-md-7 my-auto">
            <h3>{{products.name}} </h3>
            <p>{{products.subtitle}}<p>
            <span>{{(products.price / 100) | number_format(2,',','.')}} € </span>
            <hr>
            <p>
                {{products.description}}
            </p>
            <a href="{{path('add_to_cart',{'id' : products.id})}}" class="btn btn-primary"> Ajouter au panier </a>
        </div>
    </div>
{% endblock %}
