{% extends 'base.html.twig.html' %}

{% block title %}Nos produits - La boutique{% endblock %}

{% block content %}

    <div class="row">
        <div class="col-md-3">
            <h1>Filtrer</h1>
            {{form(form)}}
        </div>
        <div class="col-md-9">
            
    <h1> Nos produits</h1>
    <div class="row">
        {% for product in products %}
            <div class="col-md-4 product-container">
                <div class="product-item text-center">
                    <a href="{{path('producte',{'slug': product.slug})}}"><img src="uploads/{{product.illustration}}" class="img-fluid"></a>
                    <h5>{{product.name}}</h5>
                    <span class="product-subtitle">{{product.subtitle}}</span>
                    <span class="product-price">{{(product.price / 100) | number_format(2,',','.')}} €</span>
                </div>
            </div>
        {% endfor %}
    </div>
        </div>
    </div>
{% endblock %}
